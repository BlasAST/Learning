// Bueno esto ya lo conozco pero es que esoty en clas ey tengo que apuntarlo

#include <iostream>
#include <fstream>
int main(){
    // ifstream file;
    file.open("Ruta de donde esta el fichero");
    if(file.is_open(){

    })
    file.close();

    // Otra forma seria

    // ifstream file(path);

    // ifstream lectura de datos
    // ofstream escritura de datos
    // fsteam generico

    // String linea
    // while ( getline(fichero,linea))
    // std::cout << linea << endl;
    // while(get(file.get(char)))
    return 0;
}